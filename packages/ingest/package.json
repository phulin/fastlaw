{
	"name": "@fastlaw/ingest",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "NODE_EXTRA_CA_CERTS=certs/godaddy-g2.pem wrangler dev --persist-to ../web/.wrangler/state",
		"build:container": "RUSTC_WRAPPER=sccache docker run --rm --platform=linux/amd64 -v \"$INIT_CWD/container-rust:/work\" -v \"$HOME/.cache/cargo-cross:/cargo\" -v \"$HOME/.cache/sccache:/sccache\" rust-musl-builder cargo build --target x86_64-unknown-linux-musl --release",
		"build:container:debug": "RUSTC_WRAPPER=sccache docker run --rm --platform=linux/amd64 -v \"$INIT_CWD/container-rust:/work\" -v \"$HOME/.cache/cargo-cross:/cargo\" -v \"$HOME/.cache/sccache:/sccache\" rust-musl-builder cargo build --target x86_64-unknown-linux-musl",
		"build": "wrangler deploy --dry-run --outdir=dist",
		"deploy": "wrangler deploy",
		"typecheck": "../../node_modules/.bin/tsc --noEmit && cd container-rust && cargo check",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@cloudflare/containers": "^0.1.0",
		"@zip.js/zip.js": "^2.8.16",
		"hono": "^4.6.4",
		"htmlparser2": "^9.1.0",
		"xxhash-wasm": "^1.1.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250129.0",
		"vitest": "^3.0.0",
		"wrangler": "^4.63.0"
	}
}

# Types Guide

This directory holds global or package-level type declarations.

- Keep declarations minimal and genuinely shared.
- Do not re-export library types from here.
- If a change is really local to one module, keep the type near that module instead.

## Files

- `global.d.ts`: package-level global type declarations.
